<!DOCTYPE html>
<html>
<head>
    <title>Request Clearance - Barangay Dashboard</title>
    <link rel="stylesheet" href="css/styles.css">
    <link rel="stylesheet" href="css/responsive.css">
    <style>
        .request-container {
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            margin-top: 20px;
            width: 80%;
            margin: 20px auto;
        }

        .request-container h2 {
            color: #333;
        }

        .request-container form input[type="text"],
        .request-container form textarea {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }

        .request-container form button {
            padding: 10px 20px;
            background: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background 0.3s;
        }

        .request-container form button:hover {
            background: #45a049;
        }

        .print-btn {
            display: none;
            background: #2196F3;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: background 0.3s;
        }

        .print-btn:hover {
            background: #1E88E5;
        }
    </style>
</head>
<body>
    <div class="main-container">
        <div class="main">
            <div class="request-container">
                <h2>Request New Clearance</h2>
                <form action="/requests/clearance" method="POST">
                    <input type="text" name="purpose" placeholder="Purpose of Clearance" required>
                    <textarea name="details" placeholder="Details of Request" rows="5" required></textarea>
                    <button type="submit">Submit Request</button>
                </form>
            </div>

            <% if (clearance && clearance.status === 'approved') { %>
            <div class="request-container" id="print-clearance">
                <h2>Barangay Clearance</h2>
                <p><strong>Purpose:</strong> <%= clearance.purpose %></p>
                <p><strong>Details:</strong> <%= clearance.details %></p>
                <p><strong>Requested By:</strong> <%= clearance.requested_by %></p>
                <p><strong>Date:</strong> <%= clearance.date %></p>
                <button class="print-btn" onclick="window.print();">Print Clearance</button>
            </div>
            <% } %>
        </div>
    </div>
</body>
</html>
